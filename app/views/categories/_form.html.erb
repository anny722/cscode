<%= simple_form_for (category), wrapper: :inline_form, html: { class: 'form-inline' } do |f| %>
  <%= f.input :name, placeholder: 'Category name', label: false, class: 'ml-3' %>
  <%= f.submit "New Category", class: "btn btn-outline-primary" %>
<% end %>
